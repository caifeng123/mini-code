;(function(map){
  const __webpack_module_cache__ = {};
  const require = (fileID) => {
    if(__webpack_module_cache__[fileID]) {
      return __webpack_module_cache__[fileID].exports;
    }
    const [fn, mapping] = map[fileID];
    
    const module = (__webpack_module_cache__[fileID] = {
      exports: {}
    });

    const localRequire = (filename) => {
      return require(mapping[filename]);
    }
    
    fn(localRequire, module, module.exports);
    return module.exports;
  }
  require(0);
})({
  <% data.forEach((info) => { %>
    <%- info.id %>: [function(require, module, exports) {
      <%- info.code  %>
    }, <%- JSON.stringify(info.mapping)  %>],
  <% }); %>
});
